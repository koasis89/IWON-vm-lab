trigger:
  branches:
    include:
    - main
  paths:
    include:
    - backup/**
    - vm-ansible/**
    - vm-azure/**
    - devops/azure-pipelines-vm.yml
pr: none

parameters:
- name: runTerraform
  displayName: Run Terraform Apply
  type: boolean
  default: true

- name: deployTarget
  displayName: Ansible Deploy Target
  type: string
  default: all
  values:
  - all
  - web
  - was
  - app
  - integration
  - db
  - kafka

- name: applyDbMigration
  displayName: Apply DB SQL Migration
  type: boolean
  default: false

- name: dbMigrationSqlPath
  displayName: DB migration SQL path (repo relative)
  type: string
  default: backup/db/migration.sql

variables:
- name: TF_WORKING_DIR
  value: vm-azure
- name: ANSIBLE_WORKING_DIR
  value: vm-ansible
- name: ANSIBLE_INVENTORY_OUT
  value: $(Pipeline.Workspace)/generated/inventory.generated.ini
- name: TF_OUTPUT_JSON
  value: $(Pipeline.Workspace)/generated/tf-output.json
- name: ANSIBLE_SSH_USER
  value: iwon
- name: ANSIBLE_SSH_KEY_PATH
  value: ~/.ssh/id_rsa

# Optional override values for release assets.
- name: WEB_HTML_ZIP_PATH
  value: backup/dev-web/html.zip
- name: WAS_JAR_PATH
  value: backup/dev-was/GodisWebServer-0.0.1-SNAPSHOT.jar
- name: APP_JAR_PATH
  value: backup/dev-app/godisappserver-0.0.1-SNAPSHOT.jar
- name: INTEGRATION_JAR_PATH
  value: backup/dev-integration/IWonPaymentIntegration-0.0.1-SNAPSHOT.jar

# Backend configuration variables (set these in pipeline variables or variable group).
- name: TFSTATE_RG
  value: ""
- name: TFSTATE_STORAGE
  value: ""
- name: TFSTATE_CONTAINER
  value: tfstate
- name: TFSTATE_KEY
  value: vm-azure/terraform.tfstate

stages:
- stage: Terraform
  displayName: Stage 1 - Terraform Apply and Output
  condition: and(succeeded(), eq('${{ parameters.runTerraform }}', true))
  jobs:
  - job: TerraformApply
    displayName: Terraform Validate/Plan/Apply
    pool:
      vmImage: ubuntu-latest
    steps:
    - checkout: self

    - task: TerraformInstaller@1
      displayName: Install Terraform
      inputs:
        terraformVersion: latest

    - task: AzureCLI@2
      displayName: Terraform Init/Validate/Plan/Apply
      inputs:
        azureSubscription: $(AZURE_SERVICE_CONNECTION)
        scriptType: bash
        scriptLocation: inlineScript
        inlineScript: |
          set -euo pipefail

          if [ -n "$(TFSTATE_RG)" ] && [ -n "$(TFSTATE_STORAGE)" ]; then
            terraform -chdir="$(TF_WORKING_DIR)" init \
              -backend-config="resource_group_name=$(TFSTATE_RG)" \
              -backend-config="storage_account_name=$(TFSTATE_STORAGE)" \
              -backend-config="container_name=$(TFSTATE_CONTAINER)" \
              -backend-config="key=$(TFSTATE_KEY)"
          else
            terraform -chdir="$(TF_WORKING_DIR)" init
          fi

          terraform -chdir="$(TF_WORKING_DIR)" validate
          terraform -chdir="$(TF_WORKING_DIR)" plan -out=tfplan
          terraform -chdir="$(TF_WORKING_DIR)" apply -auto-approve tfplan

          RG_NAME=$(terraform -chdir="$(TF_WORKING_DIR)" output -raw resource_group_name)
          echo "Azure Portal: https://portal.azure.com/#@/resource/subscriptions/$(az account show --query id -o tsv)/resourceGroups/${RG_NAME}/overview"

          mkdir -p "$(Pipeline.Workspace)/generated"
          terraform -chdir="$(TF_WORKING_DIR)" output -json > "$(TF_OUTPUT_JSON)"

    - publish: $(TF_OUTPUT_JSON)
      artifact: terraform-output
      displayName: Publish Terraform output JSON

- stage: Ansible
  displayName: Stage 2 - Generate Inventory and Configure VMs
  dependsOn: Terraform
  condition: or(and(eq('${{ parameters.runTerraform }}', true), succeeded('Terraform')), eq('${{ parameters.runTerraform }}', false))
  jobs:
  - job: ConfigureVms
    displayName: Run Ansible on Managed Agent
    pool:
      vmImage: ubuntu-latest
    steps:
    - checkout: self

    - task: TerraformInstaller@1
      displayName: Install Terraform (for output export)
      inputs:
        terraformVersion: latest

    - task: DownloadPipelineArtifact@2
      displayName: Download Terraform output artifact
      condition: eq('${{ parameters.runTerraform }}', true)
      inputs:
        artifact: terraform-output
        path: $(Pipeline.Workspace)/downloaded-terraform

    - task: AzureCLI@2
      displayName: Export Terraform outputs when Terraform stage is skipped
      condition: eq('${{ parameters.runTerraform }}', false)
      inputs:
        azureSubscription: $(AZURE_SERVICE_CONNECTION)
        scriptType: bash
        scriptLocation: inlineScript
        inlineScript: |
          set -euo pipefail
          terraform -chdir="$(TF_WORKING_DIR)" init
          mkdir -p "$(Pipeline.Workspace)/generated"
          terraform -chdir="$(TF_WORKING_DIR)" output -json > "$(TF_OUTPUT_JSON)"

    - bash: |
        set -euo pipefail
        mkdir -p "$(Pipeline.Workspace)/generated"
        if [ -f "$(Pipeline.Workspace)/downloaded-terraform/tf-output.json" ]; then
          cp "$(Pipeline.Workspace)/downloaded-terraform/tf-output.json" "$(TF_OUTPUT_JSON)"
        fi

        python3 "$(Build.SourcesDirectory)/devops/scripts/generate_inventory_from_tf.py" \
          --tf-output "$(TF_OUTPUT_JSON)" \
          --output "$(ANSIBLE_INVENTORY_OUT)" \
          --ssh-user "$(ANSIBLE_SSH_USER)" \
          --ssh-key-path "$(ANSIBLE_SSH_KEY_PATH)"
      displayName: Generate dynamic inventory from Terraform outputs

    - publish: $(ANSIBLE_INVENTORY_OUT)
      artifact: ansible-inventory
      displayName: Publish generated inventory

    - task: InstallSSHKey@0
      displayName: Install SSH private key
      inputs:
        knownHostsEntry: ''
        sshPublicKey: ''
        sshKeySecureFile: $(ANSIBLE_SSH_PRIVATE_KEY_SECURE_FILE)

    - bash: |
        set -euo pipefail
        sudo apt-get update
        sudo apt-get install -y ansible python3-pip
        ansible --version
      displayName: Install Ansible

    - bash: |
        set -euo pipefail
        export ANSIBLE_HOST_KEY_CHECKING=False
        export ANSIBLE_CONFIG="$(Build.SourcesDirectory)/$(ANSIBLE_WORKING_DIR)/ansible.cfg"

        EXTRA_VARS=(
          "was_jar_src=$(Build.SourcesDirectory)/$(WAS_JAR_PATH)"
          "app_jar_src=$(Build.SourcesDirectory)/$(APP_JAR_PATH)"
          "integration_jar_src=$(Build.SourcesDirectory)/$(INTEGRATION_JAR_PATH)"
          "web_html_zip_src=$(Build.SourcesDirectory)/$(WEB_HTML_ZIP_PATH)"
          "db_password=$(DB_APP_PASSWORD)"
          "db_root_password=$(DB_ROOT_PASSWORD)"
        )

        if [ "${{ parameters.deployTarget }}" = "all" ]; then
          ansible-playbook \
            -i "$(ANSIBLE_INVENTORY_OUT)" \
            "$(Build.SourcesDirectory)/$(ANSIBLE_WORKING_DIR)/site.yml" \
            --extra-vars "${EXTRA_VARS[*]}"
        else
          ansible-playbook \
            -i "$(ANSIBLE_INVENTORY_OUT)" \
            "$(Build.SourcesDirectory)/$(ANSIBLE_WORKING_DIR)/site.yml" \
            --limit "${{ parameters.deployTarget }}" \
            --extra-vars "${EXTRA_VARS[*]}"
        fi
      displayName: Run Ansible site playbook

    - bash: |
        set -euo pipefail
        export ANSIBLE_HOST_KEY_CHECKING=False
        export ANSIBLE_CONFIG="$(Build.SourcesDirectory)/$(ANSIBLE_WORKING_DIR)/ansible.cfg"

        ansible-playbook \
          -i "$(ANSIBLE_INVENTORY_OUT)" \
            "$(Build.SourcesDirectory)/devops/ansible/deploy-db-sql.yml" \
          --extra-vars "db_sql_migration_file=$(Build.SourcesDirectory)/${{ parameters.dbMigrationSqlPath }} db_root_password=$(DB_ROOT_PASSWORD)"
      displayName: Apply DB SQL migration
      condition: eq('${{ parameters.applyDbMigration }}', true)
